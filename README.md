📝 Flask Blog API + React (Vite) Frontend
A full-stack blog application built using Flask (Python) for the backend REST API and React + Vite for the frontend.
The app supports CRUD operations (Create, Read, Update, Delete) for blog posts.

🚀 Features

Backend (Flask)
  RESTful API
  SQLite database using SQLAlchemy
  CRUD operations:
  GET /blogs – fetch all blogs
  GET /blogs/<id> – fetch single blog
  POST /blogs – create a blog
  PUT /blogs/<id> – update a blog
  DELETE /blogs/<id> – delete a blog
  JSON responses
  CORS enabled for frontend integration

  
Frontend (React + Vite)
  Built with React (JavaScript)
  Uses Axios for API calls
  Displays all blog posts
  Create, Update, Delete blogs from UI
  Fast development with Vite

🛠 Tech Stack
Backend
Python
Flask
Flask-SQLAlchemy
Flask-CORS
SQLite

Frontend
React
Vite
Axios
(Optional) Tailwind CSS

